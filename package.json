{
  "name": "pvi-json-server",
  "version": "1.0.0",
  "description": "pvi json mock server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch --host https://pvi-db.herokuapp.com  db.json"
  },
  "keywords": [
    "json-server",
    "mock"
  ],
  "author": "Daniel Nsanzumuhire",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.2"
  },
  "devDependencies": {
    "json-server": "^0.16.2"
  }
}